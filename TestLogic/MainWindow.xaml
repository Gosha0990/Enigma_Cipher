<Window x:Class="TestLogic.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"       
        xmlns:local="clr-namespace:TestLogic.MVVM.ViewModel"
        xmlns:vievModel="clr-namespace:TestLogic.MVVM.ViewModel.Base"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:PersonData/>
    </Window.DataContext>
    <Window.Resources>
        
    </Window.Resources>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
            <Label>Customer Name: </Label>
            <TextBox Text="{Binding Name}" Width="136"/>
            <Label Content="{Binding Name}"/>
        <TextBox Text="0" Width="136"/>
        <Button 
                Content="Start" > 
                
        </Button>
    </StackPanel>
    
</Window>
